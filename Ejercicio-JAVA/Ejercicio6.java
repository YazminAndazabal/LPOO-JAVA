 package yaz;
/*Lee un número por teclado e indica si es divisible entre 2 (resto = 0). Si no lo es, también debemos indicarlo.*/

import javax.swing.JOptionPane;//libreria para la ventana output

public class yaz {
	public static void main(String[] args) {
		String a = JOptionPane.showInputDialog("radio");//ventana con mensaje
		int radio= Integer.parseInt(a);
		JOptionPane.showMessageDialog(null, "a"  );

	}
}
